<header>
  <h1><a href="/"><span class="title">Joplin.</span> It's RAG time for your laptop.</a></h1>
</header>

<style>
  header {
    background-color: var(--color-dark);
    padding: calc(var(--unit) * 0.5) calc(var(--unit) * 2);
    position: fixed;
    width: 100%;
    z-index: 10;
  }
  header h1 {
    color: var(--color-light);
    font-family: var(--font-sans);
    font-size: var(--unit);
    font-weight: 100;
  }
  header h1 a .title {
    color: var(--color-light);
    font-weight: 800;
  }
  header h1 a {
    color: var(--color-light);
    text-decoration: none;
  }
</style>
